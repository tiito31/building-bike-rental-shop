./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./B
./bike-shop.sh
echo '28 | Mountain'
echo '28 | Mountain'
echo '28 | Mountain'
echo '28 | Mountain'
echo '28 | Mountain'
echo '28 Mountain'
echo '28 Mountain'
echo '28 | Mountain'
echo '28 | Mountain'
echo '28 | Mountain'
./bike-shop.sh
./bike-shop.sh
echo ' M e '
echo "$(echo ' M e ')."
echo "$(echo ' M e ' | sed 's/ //')."
echo "$(echo ' M e ' | sed 's/ //g')."
echo "$(echo ' M e ' | sed 's/ ^//g')."
echo "$(echo ' M e ' | sed 's/ /^/g')."
echo "$(echo ' M e ' | sed 's/^ //g')."
echo "$(echo ' M e ' | sed 's/^ $ //g')."
echo "$(echo ' M e ' | sed 's/^ //g')."
echo "$(echo ' M e ' | sed 's/^* //g')."
echo "$(echo ' M e ' | sed 's/^ *//g')."
echo "$(echo ' M e ' | sed 's/^$ *//g')."
echo "$(echo ' M e ' | sed 's/ $//g')."
echo "$(echo ' M e ' | sed 's/ $//g')."
echo "$(echo ' M e ' | sed 's/ $//g')."
echo "$(echo ' M e ' | sed 's/ $//g')."
echo "$(echo ' M e ' | sed 's/^ $//g')."
echo "$(echo ' M e ' | sed 's/ *$//g')."
echo "$(echo ' M e ' | sed 's/^ *| *$//g')."
echo "$(echo ' M e ' | sed -E 's/^ *| *$//g')."
man sed
echo "$(echo ' M e ' | sed 's/^ *| *$//g')."
echo "$(echo ' M e ' | sed -E 's/^ *| *$//g')."
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
./bike-shop.sh
echo "# learning build bike shop rental" >> README.md
git init
git add .
git commit -m "final commit"
git branch -M main
git remote set-url origin https://github.com/tiito31/building-bike-rental-shop.git
git push -u origin main
git fetch
git pull
git pull origin main
git push -u origin main
git pull origin main
git push -u origin main
git fetch
git rebase origin main
git rebase main
git commit -a
git push originmain
git push origin main
git pull origin main
git fetch origin
git merge origin main
git merge main
git push -u origin main
git pull --rebase origin main
git commit -m "final commit"
git add .
git commit -m "final commit"
git pull --rebase origin main
git commit -m "final commit"
git add .
